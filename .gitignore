# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
.env
.env.*
!.env.example

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist
.output

# Gatsby files
.cache/
# Comment in the public line in if your project uses Gatsby and not Next.js
# https://nextjs.org/blog/next-9-1#public-directory-support
# public

# vuepress build output
.vuepress/dist

# vuepress v2.x temp directory
.temp

# Sveltekit cache directory
.svelte-kit/

# vitepress build output
**/.vitepress/dist

# vitepress cache directory
**/.vitepress/cache

# Docusaurus cache and generated files
.docusaurus

# Serverless directories
.serverless/

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# Firebase cache directory
.firebase/

# TernJS port file
.tern-port

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# pnpm
.pnpm-store

# yarn v3
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite files
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.vite/

# ---------------------------------------------------------------------------
# template-dlab5-net
# ---------------------------------------------------------------------------

# Gatsby
.cache/
public/

# Amplify Gen 2. amplify_outputs.json is generated by `ampx sandbox` or by
# `ampx pipeline-deploy` and copied into packages/site/src at build time — it
# is environment state, not source, so it is never committed.
amplify_outputs.json
amplify_outputs.d.ts
.amplify/

# Workspace package build output. packages/site imports @dlab5/app-core from
# dist/, which is why `npm run build` builds core before the site.
packages/*/dist/

# JetBrains
.idea/

# Scratch space for specs and notes that are not yet part of the repo
temp/

# scripts/dev.sh writes the sandbox's output here so the terminal stays
# readable. Local, and regenerated on every run.
.dev-sandbox.log

# -- Python ----------------------------------------------------------------
# scripts/demo.py is run, never imported, so nothing here is needed by anyone.
# Bytecode appears the moment the file is compiled or imported once.
__pycache__/
*.py[cod]

# ---------------------------------------------------------------------------
# Secrets hygiene — ASSUME THIS REPOSITORY IS PUBLIC.
#
# Nothing below is ever committed. Enable GitHub secret scanning and push
# protection on the fork as a backstop, but the backstop is not the plan.
#
# Also never commit, even though they are not matched by a pattern:
#   - AWS account IDs and ARNs in code, docs or ADRs
#   - Cognito user-pool / identity-pool / AppSync IDs (that is why
#     amplify_outputs.json is ignored above)
#   - customer or colleague names, emails and internal hostnames in seed data
# GATSBY_*-prefixed environment variables are inlined into the public bundle
# at build time. Never put a secret behind a GATSBY_ prefix.
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
id_rsa*
credentials
aws-exports.js

# Regenerated by Gatsby on every develop/build (graphqlTypegen). Nothing
# depends on it at review time, so it stays out rather than churning.
packages/site/src/gatsby-types.d.ts
